from flask import url_for
from pydantic import BaseModel

from {{project_name}}.{{model_name}}.models import {{model_class}}
from {{project_name}}.base.schemas import BaseOutputSchema


class {{model_class}}InputSchema(BaseModel):
    pass

class {{model_class}}OutPutSchema(BaseOutputSchema):
    pass

class {{model_class}}UpdateSchema(BaseModel):
    pass